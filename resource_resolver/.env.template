# Resource Resolver — environment variables
# Copy this to .env and fill in real values
# Load with: export $(grep -v '^#' .env | xargs)

# HPE OneView
HPE_OV_HOST=oneview.mgmt.local
HPE_OV_USER=administrator
HPE_OV_PASS=changeme
HPE_OV_SSL=true
HPE_OV_API_VERSION=3000

# HPE Compute Ops
COMPUTE_OPS_USER=api-user

# Cache
CACHE_TTL=300

# Vault (production)
VAULT_ADDR=https://vault.mgmt.local:8200
VAULT_TOKEN=
