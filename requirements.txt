mcp>=0.1.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter
redis>=5.0.0            # Redis cache backend

# Optional future dependencies:
# requests>=2.31.0   # Only needed when you add real HPE OneView / Compute Ops HTTP calls
